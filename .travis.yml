language: python
python:
  - "2.6"
  - "2.7"
# command to install dependencies
install:
  - touch README.txt
  - pip install .
  - pip install -r requirements.txt
# command to run tests
script: make check
